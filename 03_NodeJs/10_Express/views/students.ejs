<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>
        table {
            border-collapse: collapse;
        }

    th, td {
        border: 1px solid black;
        font-size: 1rem;
        padding: 10px;
        text-align: center;
    }
    caption {
        font-size: 1.2rem;
        font-weight: bold;
    }
    </style>

</head>

<body>
    <h1>Hello EJS!</h1>
    <!-- render传递进来的参数可以直接访问(借助特定标签) -->
    <!-- 在服务器控制台输出 -->

    <!-- 
        1、html正常编写
        2、js代码直接写在 <% %> 之中
        3、ejs注释为 <%# %>
        4、动态变量: <%= %> ，动态参数
        5、可以嵌套编写，成对标签内是ejs的js代码，标签外是html
     -->

    <%# 
        console.log(stus) 
        for (const stu of stus) { 
        console.log(stu.name, stu.age, stu.gender) 
         } 
    %>

    <!-- 在客户端页面输出 -->
    <% for (const stu of stus) { %>
        <%= stu.name %> - <%= stu.age %> - <%= stu.gender %> - <%= stu.address %>
        <br>
    <% } %>

    <br>
    <!-- 将数据呈现在表格里 -->
    <table>
        <caption>学生列表</caption>
        <thead>
            <tr>
                <th>学号</th>
                <th>姓名</th>
                <th>年龄</th>
                <th>性别</th>
                <th>地址</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>1</td>
                <td>孙悟空</td>
                <td>18</td>
                <td>男</td>
                <td>花果山</td>
            </tr>
        </tbody>
    </table>

</body>

</html>