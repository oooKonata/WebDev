<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>修改学生信息</title>
</head>
<body>
    <h3>修改学生信息</h3>
    <form action="/add-student" method="post">
        <div>
            姓名：<input type="text" name="<%= updateStu.name %>">
        </div>
        <div>
            性别：<input type="radio" name="gender" value="男" checked>男
            <input type="radio" name="gender" value="女">女
        </div>
        <div>
            年龄：<input type="number" min="0" max="200" name="age" value="<%= updateStu.age %>">
        </div>
        <div>
            住址：<input type="text" name="address" value="<%= updateStu.address %>">
        </div>
        <div>
            <button>修改</button>
        </div>
</body>
</html>