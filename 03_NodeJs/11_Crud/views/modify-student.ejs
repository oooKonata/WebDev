<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>修改学生信息</title>
</head>

<body>
    <h3>修改学生信息</h3>
    <form action="/update-student" method="post">
        <div>
            <input hidden type="text" name="id" value="<%= modifyStu.id %>">
        </div>
        <div>
            姓名：<input type="text" name="name" value="<%= modifyStu.name %>">
        </div>
        <div>
            <!-- 
                理解：
                modifyStu.gender === "男" && "checked"
                性别为“男”时，等价于 true && "checked" 等价于 "checked"
             -->
            性别：<input type="radio" name="gender" value="男" <%=modifyStu.gender==="男" && "checked" %>>男
            <input type="radio" name="gender" value="女" <%=modifyStu.gender==="女" && "checked" %>>女
        </div>
        <div>
            年龄：<input type="number" min="0" max="200" name="age" value="<%= modifyStu.age %>">
        </div>
        <div>
            住址：<input type="text" name="address" value="<%= modifyStu.address %>">
        </div>
        <div>
            <button>修改</button>
        </div>
</body>

</html>